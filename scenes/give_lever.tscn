[gd_scene load_steps=3 format=2]

[ext_resource path="res://scenes/lever.tscn" type="PackedScene" id=1]

[sub_resource type="GDScript" id=1]
script/source = "extends Node2D

signal give_resource

export var RESOURCE_A_SECOND = 1

onready var lever = $lever


export var resource_name = \"power\"
export var is_on : bool

# Called when the node enters the scene tree for the first time.
func _ready():
	lever.is_on = self.is_on


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	if lever.is_on :
		emit_signal(\"give_resource\", resource_name, RESOURCE_A_SECOND * delta)
"

[node name="give_lever" type="Node2D" groups=[
"give_resource",
]]
script = SubResource( 1 )

[node name="lever" parent="." instance=ExtResource( 1 )]
