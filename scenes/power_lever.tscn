[gd_scene load_steps=3 format=2]

[ext_resource path="res://scenes/lever.tscn" type="PackedScene" id=1]

[sub_resource type="GDScript" id=1]
script/source = "extends Node2D

signal GivePower

const POWER_A_SECOND = 5

onready var lever = $lever

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	if lever.is_on :
		emit_signal(\"GivePower\", POWER_A_SECOND * delta)
"

[node name="power_lever" type="Node2D" groups=[
"powersource",
]]
script = SubResource( 1 )

[node name="lever" parent="." instance=ExtResource( 1 )]
